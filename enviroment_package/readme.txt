1.运行create_log.bat自动在C盘创建Anaconda文件夹  [点击 create_log.bat文件即可]
	默认路径为： C:\Anaconda

2.将Miniconda2-latest-Windows-x86_64.exe安装到第1步创建的文件夹下，其他步骤一直下一步
	注意这里的安装目录请确保和上面路径保持一致，否则第三步无法生效
	
3.以管理员的身份运行install_env.bat  [点击 install_env.bat文件即可]

4.环境安装完毕

注意如果您还未安装Chrome浏览器，请下载安装即可，否则将服务启动程序